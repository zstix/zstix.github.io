dir="$HOME/.tmp"
file="bf.jpg"
remote="http://zackstickles.com/$file"
local="$dir/$file"

if [ ! -d $dir ]; then
  mkdir $dir
fi

curl -s $remote -o $local

# TODO: get this to work
osascript -e 'tell application "Finder" to set desktop picture to POSIX file "'$path'"'
